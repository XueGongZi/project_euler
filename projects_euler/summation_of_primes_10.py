import 3_largest_prime_factor as prime

class Solution:
    def summation_of_primes(self, limit):
        # limit is the upper limit
        prime.is_prime(13)

print(Solution().summation_of_primes())
